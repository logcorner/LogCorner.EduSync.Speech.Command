apiVersion: apps/v1
kind: Deployment

metadata:
  name: command-api-deployment
  namespace: dev
  labels:
    run: command-api

spec:
  replicas: 1
  selector:
    matchLabels:
      run: command-api
  
  template:
    metadata:
      labels:
        run: command-api
    spec:
      containers:
      - name: command-api-container
        image: logcornerhub/logcorner-edusync-speech-command:1.0.0
        imagePullPolicy: Never
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Kubernetes"
        ports:
        - containerPort: 80
        - containerPort: 443
          protocol: TCP
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
    